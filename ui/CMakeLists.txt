# UI library - stub for Phase 1
# Full Qt UI implementation will be added in Phase 1 completion

# For now, this is an empty placeholder
# The UI will depend on Qt6 when implemented

# Placeholder library so root CMake doesn't fail
add_library(freeeffect_ui INTERFACE)
